
<!-- ACCOUNT USER SHOW -->
<!-- <%# @user.get_transactions %>
<ul>
  <%# Receipt.all.each do |r| %>
    <li><%#= r.kind %><br><%#= r.cost %><br><%#= r.date %><br><br></li>
  <%# end %>
</ul> -->
 <nav class="navbar group">
      <div class="nav-inner">
        <div class="logo">Carbon Groove</a></div>
        <div class="login-right">
            <ul>
              <li class="profile"></li>
              <li><a href="#">log in</a></li>
            </ul>
        </div>
      </div>
  </nav>

<% #@user.get_transactions %>

  <div class="container">
   <div class="dashboard box-offset">
    <table id="data" style="position: absolute; left: -9999em; top: -9999em;">

      <tfoot>
        <tr>
        <% @user.intervals.each do |i| %>
          <th><%= i.start_range %></th>
        <% end %>
        </tr>
      </tfoot>
      <tbody>
        <tr>
        <% @user.intervals.each do |i| %>
          <td><%= i.total_emission %></td>
        <% end %>
        </tr>
      </tbody>
    </table>
    <div id="holder"></div>
  </div>
</div>
